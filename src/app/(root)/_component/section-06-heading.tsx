import { NewSection } from '@/src/app/(root)/_template/new-section'

export const Section06Heading = () => {
  return (
    <NewSection
      heading={'それってcss設計を放棄してるんでしょ？'}
      headingLevel={'h2'}
    />
  )
}
